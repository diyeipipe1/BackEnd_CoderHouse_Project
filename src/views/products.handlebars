<h1>Products, bienvenido {{name}}</h1> <br>
<h2>{{email}}</h2> <br>
<h2>{{role}}</h2>
<div>
    {{#if payload.length}}
        <table>
            <thead>
                <tr>
                    <th>id</th>
                    <th>title</th>
                    <th>description</th>
                    <th>price</th>
                    <th>thumbnail</th>
                    <th>code</th>
                    <th>stock</th>
                    <th>category</th>
                    <th>Add to Cart</th>
                </tr>
            </thead>
            <tbody>
                {{#each payload}}
                    <tr>
                        <td>{{id}}</td>
                        <td>{{title}}</td>
                        <td>{{description}}</td>
                        <td>{{price}}</td>
                        <td>{{thumbnail}}</td>
                        <td>{{code}}</td>
                        <td>{{stock}}</td>
                        <td>{{category}}</td>
                        <td><button onclick={addToCart("{{id}}")}>Add</button></td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    {{/if}}
</div>
<div>
    {{#if hasPrevPage}}
        <a href={{prevLink}}><button>Anterior</button></a>
    {{/if}}
    {{#if hasNextPage}}
        <a href={{nextLink}}><button>Siguiente</button></a>
    {{/if}}
</div>

<br>
<div>
    <button id="btnLogout">Logout</button> 
</div>

<script src="/js/products.js"></script>
<script src="/js/logout.js"></script>
